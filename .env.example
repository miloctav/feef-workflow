# PostgreSQL Configuration
POSTGRES_DB=feef_db
POSTGRES_USER=feef_user
POSTGRES_PASSWORD=change_me_in_production
DATABASE_URL=postgresql://feef_user:change_me_in_production@localhost:5432/feef_db

# MinIO Configuration
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=change_me_in_production
MINIO_BUCKET=feef-storage

# Domain Configuration (for SSL certificate generation)
DOMAIN=your-domain.com
EMAIL=your-email@example.com

# Session Configuration (nuxt-auth-utils)
NUXT_SESSION_PASSWORD=a-random-password-with-at-least-32-characters
